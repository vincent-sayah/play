<?php

use Illuminate\Support\Facades\Route;

Route::get('/', function () {
    return view('welcome');
});

/**
 * Endpoint d'observabilité attendu par la supervision
 * Chemin exact : {{ api_obs_info_alias_url }}
 * Réponse statique, sans vérification de la base de données.
 */
Route::get('{{ api_obs_info_alias_url }}', function () {
    return response()->json(
        [
            'message' => 'Api disponible',
            'details' => "Le système d'information est opérationnel",
        ],
        200,
        //content-type
        ['Content-Type' => 'application/problem+json']
    );
});
